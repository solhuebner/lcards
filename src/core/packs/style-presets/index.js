/**
 * Style Presets - Unified Export
 *
 * Centralized export for all style presets used by the pack system.
 * Follows the same pattern as the component registry for consistency.
 *
 * @module core/packs/style-presets
 */

export { BUTTON_PRESETS } from './buttons/index.js';
export { SLIDER_PRESETS } from './sliders/index.js';
