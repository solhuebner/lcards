# Validation

!!! note "Placeholder"
    This page is a placeholder. Full content coming soon.

LCARdS validates card configuration at runtime using the **Validation Service** (`window.lcards.core.validationService`).

## Overview

The validation service checks card configuration against a schema and reports errors in the browser console and (where applicable) in the card's error state.
